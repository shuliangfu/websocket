# CI: 三端跨平台测试（Deno / Bun / Node.js 22+），验证跨运行时兼容性
# 注意：tests/adapters-mongodb.test.ts 与 tests/adapters-redis.test.ts 为集成测试
#       （需 Docker MongoDB 27017 与 Redis 6379），CI 仅运行 14 个单元测试文件
#       （真实进程内 WebSocket server+client，使用默认 MemoryAdapter，无外部服务）。
name: CI

on:
  push:
    branches: [dev]
  pull_request:
    branches: [dev]

# 工作流级 env：让基于 Node 的 GitHub Actions（如 checkout、setup-*）提前使用 Node 24 运行时，
# 便于在官方强制升级前做性能/稳定性验证。参见 GitHub 文档中的 FORCE_JAVASCRIPT_ACTIONS_TO_NODE24。
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  # ─────────────────────────────────────────────────────────────────────────
  # Deno（Linux / macOS / Windows）
  # ─────────────────────────────────────────────────────────────────────────
  test-deno:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.9

      - name: Cache dependencies
        continue-on-error: true
        run: deno cache src/mod.ts

      - name: Check
        run: deno check src/mod.ts

      - name: Lint
        run: deno lint src/

      - name: Test (Deno)
        run: deno test -A tests/connection.test.ts tests/data-storage.test.ts tests/disconnect.test.ts tests/encryption.test.ts tests/error-handling.test.ts tests/heartbeat.test.ts tests/logger-debug-i18n.test.ts tests/middleware.test.ts tests/namespace.test.ts tests/optimization.test.ts tests/room.test.ts tests/runtime-compat.test.ts tests/server.test.ts tests/socket-events.test.ts

  # ─────────────────────────────────────────────────────────────────────────
  # Bun（Linux / macOS / Windows）
  # ─────────────────────────────────────────────────────────────────────────
  test-bun:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install

      - name: Test (Bun)
        run: bun test tests/connection.test.ts tests/data-storage.test.ts tests/disconnect.test.ts tests/encryption.test.ts tests/error-handling.test.ts tests/heartbeat.test.ts tests/logger-debug-i18n.test.ts tests/middleware.test.ts tests/namespace.test.ts tests/optimization.test.ts tests/room.test.ts tests/runtime-compat.test.ts tests/server.test.ts tests/socket-events.test.ts

  # ─────────────────────────────────────────────────────────────────────────
  # Node.js 22（Linux / macOS / Windows）
  # ─────────────────────────────────────────────────────────────────────────
  test-node:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install dependencies
        run: npm install

      - name: Test (Node)
        run: npm run test:node
